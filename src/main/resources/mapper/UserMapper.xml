<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qlz.aibserver.mapper.UserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.qlz.aibserver.entity.User">
        SELECT id, username, password, email, nickname, avatar, status, create_time, update_time, deleted
        FROM t_user
        WHERE username = #{username} AND deleted = 0
    </select>

    <!-- 根据邮箱查询用户 -->
    <select id="selectByEmail" resultType="com.qlz.aibserver.entity.User">
        SELECT id, username, password, email, nickname, avatar, status, create_time, update_time, deleted
        FROM t_user
        WHERE email = #{email} AND deleted = 0
    </select>

    <!-- 检查用户名是否存在 -->
    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM t_user
        WHERE username = #{username} AND deleted = 0
    </select>

    <!-- 检查邮箱是否存在 -->
    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM t_user
        WHERE email = #{email} AND deleted = 0
    </select>

</mapper>
